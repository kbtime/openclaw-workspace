# 2026-02-13 记忆

## 服务器存储分析

Boss 要求分析服务器存储空间，结论如下：

### 磁盘状况
- 系统盘 `/dev/vda1`: 40G 总量，已用 32G，剩余 8.1G，使用率 **80%**

### 空间占用大户
1. **npm 缓存** `.npm/_cacache` — 5.2G (可安全清理)
2. **pip 缓存** `.cache/pip` — 1.7G (可安全清理)
3. **OpenClaw 扩展** `.openclaw/extensions/` — 3.3G
   - qqbot/node_modules: 2.1G
   - wecom/node_modules: 1.2G
4. **Playwright** `.cache/ms-playwright` — 1.2G
5. **Whisper 模型** `.cache/whisper` — 534M
6. **Swap 文件** `/swapfile` — 8G (正常配置)
7. **/tmp 临时文件** — ~1G (pip 解压残留)

### 清理建议
- `npm cache clean --force` → 释放 5.2G
- `pip cache purge` → 释放 1.7G
- `rm -rf /tmp/pip-*` → 释放 ~1G
- 总计可安全释放: **8-9G**

### 清理执行结果 ✅
- 已执行 `npm cache clean --force` → 清理 npm 缓存
- 已执行 `pip cache purge` → 删除 626 个文件
- 已执行 `rm -rf /tmp/pip-*` → 清理临时文件
- 已执行 `uv cache clean` → 释放 238.7MiB

### 清理前后对比
| 指标 | 清理前 | 清理后 |
|------|--------|--------|
| 已用 | 32G | 24G |
| 可用 | 8.1G | **17G** |
| 使用率 | 80% | **60%** |

**释放空间：约 8G** ✅

## 身份确认
- 模型: glmcode/glm-5
- Boss 通过飞书 DM 联系

---

## MCP Bridge Skill 开发 ✅

### 任务来源
Boss 发了「开发MCP skill 要求.md」文档，要求实现 MCP 桥接方案。

### 架构
```
OpenClaw → mcp-bridge skill (HTTP) → bridge.py (Python) → MCP Server (stdio)
```

### 完成的工作

1. **HTTP 网关** `/opt/mcp-bridge/bridge.py`
   - 监听 127.0.0.1:9712
   - 提供 GET /tools 和 POST /call 接口
   - 使用 MCP Python SDK 管理会话

2. **服务器配置** `/opt/mcp-bridge/servers.json`
   - 配置了 `fs` (filesystem) MCP server
   - 访问路径: `/data/workspace`

3. **OpenClaw Skill** `/root/.openclaw/workspace/skills/mcp-bridge/`
   - SKILL.md: 使用文档
   - index.js: 封装 HTTP 调用
   - 已被 OpenClaw 识别为 `✓ ready`

4. **systemd 服务** `/etc/systemd/system/mcp-bridge.service`
   - 可用 `systemctl start mcp-bridge` 启动

### 测试验证
- ✅ list_tools 返回 16 个 filesystem 工具
- ✅ write_file 写入成功
- ✅ read_text_file 读取成功
- ✅ skill 被 OpenClaw skills list 识别

### 后续可扩展
- 添加更多 MCP Server（git、sqlite 等）
- 编辑 `/opt/mcp-bridge/servers.json` 后重启 bridge
